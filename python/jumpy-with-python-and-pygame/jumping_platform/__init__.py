# We have to call this package other then "platform", otherwise Python would confuse it with built-in
# Python package "platform" which represents an operating system the Python program runs on.
# As a result, the code would not compile.
